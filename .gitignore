.DS_Store
__pycache__/
*.pyc
# never commit a real workspace into the plugin repo
workspace/
sources/
out/
*.pdf

# the fictional example ships its sample dumps
!examples/**/sources/
# workspace-only folders that must never be inside the plugin
history/
scripts/forbidden.local
