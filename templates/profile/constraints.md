# Constraints (hard limits for every skill and agent)

1. Posting, commenting, messaging, connecting and applying follow `career.json` automation mode
   and approvals. Outside those, never act on my behalf.
2. Never send email.
3. Never invent facts, numbers, titles, dates or employers. If it is not in cv-canonical.md or a fetched source, ask.
4. Never leak employer internals: <list ticket systems, internal tool names, client names under NDA>.
5. Never expose my phone number or home address on the site or LinkedIn.
6. No standing automation (cron, launchd, scheduled scans).
7. CV commits and site git push follow `approvals.cv_commit` and `approvals.site_push`.
8. The workspace folder stays private. Never add it to a public repo.
